#pragma once
#include <string>
using namespace std;

class InputGuard{

public:

    bool isEmpty(string value);

    bool validEmail(string email);

    bool validPassword(string password);

};
